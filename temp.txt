Reading STATUS.md... Current phase: Phase 3 — Identity Resolution & Hardening

I'll implement mDNS service enumeration. This means capturing service types (like `_googlecast._tcp.local`, `_airplay._tcp.local`) from mDNS responses and attaching them to devices for classification.

The work involves:
1. Adding a `services` field to `Device` and `Record`
2. Extending `mdns.rs` to return service types alongside hostnames
3. Updating `passive.rs` sniffing to extract service types from mDNS packets
4. Updating `cli/commands.rs` scan and listen to display services

Let me start with the data model changes, then work outward.

<FILE path="src/identity/device.rs">
</FILE>

<FILE path="src/discovery/mdns.rs">
</FILE>

<FILE path="src/discovery/passive.rs">
</FILE>

<FILE path="src/identity/store.rs">
</FILE>

<FILE path="src/cli/commands.rs">
</FILE>